#include <iostream>
using namespace std;

/*
-> vem da função hash: h'(k, m) = k mod m
-> serve para achar uma posição no vetor
-> sondagem linear: h(k, i, m) = (h'(k, m) + i) mod m
	-> i vai inicializar valendo 0, caso já haja uma chave nessa posição é feita
	a sondagem linear novamente sendo que ocorrerá i++, isso vai se repetir até
	a chave achar uma nova posição
*/

int main()
{
	//declaração de variáveis
	
	
	
	return 0;
}